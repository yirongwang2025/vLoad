"""
Pose estimation utilities.

This package defines a model-agnostic PoseFrame interface and provider adapters
(e.g., MediaPipe Pose) so we can swap pose stacks later without rewriting the app.
"""


