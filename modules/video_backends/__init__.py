"""
Video backend implementations.

We keep this as a real package (not only a namespace package) so imports like
`modules.video_backends.picamera2_backend` work reliably on all targets.
"""


